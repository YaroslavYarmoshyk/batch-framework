# Output

The generated report (`План СЧ.xlsx`) is written here, path configured under
`system-configuration.output` in `application.yml`. The file itself is git-ignored — only this
folder placeholder is tracked.
